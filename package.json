{
	"name": "ezreact-root",
	"repository": {
		"type": "git",
		"url": "https://github.com/ivanatias/ezreact.git"
	},
	"scripts": {
		"test:main-watch": "pnpm -C packages/main test:watch",
		"test:main-once": "pnpm -C packages/main test:once",
		"lint:all": "pnpx @biomejs/biome lint",
		"format:all": "pnpx @biomejs/biome format --write",
		"check:all": "pnpx @biomejs/biome check --write"
	},
	"license": "MIT",
	"devDependencies": {
		"@biomejs/biome": "1.8.3",
		"vitest": "1.6.0"
	}
}
